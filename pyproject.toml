[tool.black]
target-version = ['py39']

[tool.isort]
profile = 'black'

[tool.ruff]
target-version = "py39"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
pythonpath = [
    "../..",
    "tests/stubs",
]
