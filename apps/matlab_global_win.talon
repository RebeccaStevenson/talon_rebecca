os: win
-
mat run:
    key(ctrl-s)
    user.switcher_focus('matlab')
    key(ctrl-shift-0)
    key(f5)
    key(ctrl-0)
    #//TODO: run and debug mode, run line in matlab

mat command:
    user.switcher_focus('matlab')
    key(ctrl-0)

mat editor:
    user.switcher_focus('matlab')
    key(ctrl-shift-0)
