os: mac
mode: all
-

# Space-based SuperWhisper hotkeys are intentionally disabled because they
# conflict with Spotlight shortcuts used by `spot show` / `spot menu`.
# Use the dedicated non-conflicting bindings in `superwhisper_hotkeys.talon`
# or the spoken `whisper ...` commands instead.
    
