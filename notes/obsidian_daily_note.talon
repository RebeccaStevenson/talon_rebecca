# Obsidian daily note workflow — open, timestamp, dictate, verify.
# Global commands (not scoped to Obsidian) so "daily note" can launch from anywhere.
-
daily note: user.daily_note_start()
daily note check: user.daily_note_check()
